---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "ubiops_metric Resource - ubiops"
subcategory: ""
description: |-
  Manages a custom metric in a UbiOps project.
---

# ubiops_metric (Resource)

Manages a custom metric in a UbiOps project.

## Example Usage

```terraform
resource "ubiops_metric" "example" {
  project_name = "my-project"
  name         = "my-custom-metric"
  description  = "Tracks custom events"
  metric_type  = "GAUGE"
  unit         = "count"
  labels       = ["environment", "region"]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `metric_type` (String) Type of the metric
- `name` (String) Name of the metric
- `project_name` (String) The name of the project.

### Optional

- `description` (String) Description of the metric
- `labels` (List of String) A list of labels that can be used to get data points containing the metric
- `unit` (String) Unit of the metric

### Read-Only

- `creation_date` (String) The date when the metric was created
- `custom` (Boolean) A boolean indicating whether the metric is custom
- `id` (Number) Unique identifier for the metric
- `last_updated` (String) The date when the metric was last updated

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import ubiops_metric.example my-project/my-custom-metric
```
